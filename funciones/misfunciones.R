# Funciones
media_geom <- function (valores) {
  n <- length(valores)
  multiplicacion <- 1
  for (var in numeros) {
    # print(var)
    multiplicacion <- multiplicacion * var
  }
  multiplicacion
  media_geometrica <- multiplicacion ^ (1/n)
  media_geometrica
}